# Tools

This folder contains local editing engines and helper repos used by the agent.

- `video-use/` - the core conversational video-editing engine and helper scripts.

The compatibility path `/home/stanley/Developer/video-use` points back to
`/home/stanley/video-editing/tools/video-use`.
